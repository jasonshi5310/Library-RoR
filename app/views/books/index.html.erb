<h1>Here is all the Book in the Library:<h1>
<br>
<table>
    <tr>
        <th>Title</th>
        <th>Author</th>
        <th>ISBN</th>
        <th>Copies</th>
        <th>Link</th>
    </tr>
    <br>
    <% @books.each do |book| %>
    <tr>
        <td><%= book.title %></td>
        <td><%= book.author %></td>
        <td><%= book.isbn%></td>
        <td><%= book.copies%></td>
        <td style="padding-right: 10px"><%= link_to 'Show', book_path(book) %></td>
        <td style="padding-right: 10px"><%= link_to 'Edit', edit_book_path(book) %></td>
        <td style="padding-right: 10px"><%= link_to 'Checkout', checkout_path(book), data:{Confirm: 'Are you sure to check this book out?'} %></td>
    </tr>
    <%end%>
</table>
<div> <%= link_to "Back", administrator_index_path %> </div>